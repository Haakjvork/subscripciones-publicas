<header>
	<a class="brand" href="#">Calculadora Fiscal</a>
	<span class="tag">Mockup de estimación mensual y anual</span>
</header>

<style>
	header {
		border: 1px solid var(--line);
		border-radius: 20px;
		background: rgb(253 250 243 / 86%);
		backdrop-filter: blur(6px);
		box-shadow: var(--shadow);
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding: 0.9rem 1.2rem;
		margin-bottom: 1rem;
	}

	.brand {
		font-weight: 700;
		letter-spacing: 0.03em;
		font-size: 1.05rem;
	}

	.tag {
		font-size: 0.9rem;
		color: var(--ink-soft);
	}

	@media (max-width: 560px) {
		header {
			flex-direction: column;
			align-items: flex-start;
			gap: 0.2rem;
		}
	}
</style>
